{{- if .Values.sealed }}
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: {{ include "app-template.fullname" . }}-secrets
  namespace: {{ .Release.Namespace }}
spec:
  encryptedData:
    {{- range $key, $value := .Values.sealed }}
    {{ $key }}: {{ $value }}
    {{- end }}
{{- end }}
